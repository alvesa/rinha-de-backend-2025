services:
  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - '9999:9999'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - backend
    depends_on:
      - payment-01
      - payment-02
    deploy:
      resources:
        limits:
          cpus: '0.3'
          memory: '50MB'
  payment-01: &payment-01 # image: payment-processor:latest
    build:
      context: ../payments
      dockerfile: Dockerfile
    container_name: payment-01
    hostname: payment-01
    expose:
      - '8080'
    environment:
      - PORT=8080
      - BASE_URL_PAYMENT_PROCESSOR=http://payment-processor-default:8080
      - BASE_URL_PAYMENT_PROCESSOR_FALLBACK=http://payment-processor-fallback:8080
    networks:
      - backend
      - payment-processor
    deploy:
      resources:
        limits:
          cpus: '0.6'
          memory: '150MB'
  payment-02:
    <<: *payment-01
    container_name: payment-02
    hostname: payment-02
    deploy:
      resources:
        limits:
          cpus: '0.6'
          memory: '150MB'
networks:
  backend:
    driver: bridge
    name: backend
  payment-processor:
    external: true
