export class PaymentSummaryResponse {
  default: {
    totalRequests: number;
    totalAmount: number;
  };
  fallback: {
    totalRequests: number;
    totalAmount: number;
  };
  myDefault: {
    totalRequests: number;
    totalAmount: number;
  };
  myFallback: {
    totalRequests: number;
    totalAmount: number;
  };
}
